version: 2

models:
  - name: clients
    description: "Modèle de données clients"

metrics:
  - name: nombre_clients
    label: "Nombre total de clients"
    model: ref('clients')
    description: "Nombre de clients enregistrés"
    type: count
    sql: id
    timestamp: created_at
    time_grains: [day, month, year]
